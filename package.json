{
  "name": "neba-token",
  "version": "1.0.0",
  "description": "NEBA Token Smart Contract - Phase 1 Implementation",
  "main": "index.js",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "test:gas": "REPORT_GAS=true hardhat test",
    "test:phase2-negative": "hardhat test test/Phase2NegativeTests.test.js",
    "deploy:phase2-atomic": "hardhat run scripts/deploy-phase2-atomic.js --network base-sepolia",
    "test:phase2": "hardhat test test/Phase2Tests.test.js",
    "test:phase2-negative": "hardhat test test/Phase2NegativeTests.test.js",
    "verify": "hardhat verify",
    "lint": "npx prettier --write .",
    "audit:slither": "slither contracts/ --config-file slither.config.json",
    "audit:coverage": "hardhat coverage --threshold 95",
    "audit:all": "npm run audit:slither && npm run audit:coverage",
    "ci": "npm run build && npm run lint && npm run audit:slither && npm run test && npm run test:coverage"
  },
  "keywords": [
    "ethereum",
    "smart-contracts",
    "erc20",
    "token",
    "defi",
    "security",
    "compliance"
  ],
  "author": "NEBA Development Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@nomicfoundation/hardhat-verify": "^2.1.1",
    "@openzeppelin/contracts": "^5.4.0",
    "@openzeppelin/contracts-upgradeable": "^5.4.0",
    "@openzeppelin/hardhat-upgrades": "^3.9.1",
    "hardhat": "^2.26.3",
    "hardhat-gas-reporter": "^2.3.0",
    "solidity-coverage": "^0.8.16"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
